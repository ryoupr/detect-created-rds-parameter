{
  "name": "rds-parameter-audit-lambda",
  "version": "1.0.0",
  "description": "Lambda function to audit RDS parameter groups for EBS encryption",
  "main": "rds-parameter-audit.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-rds": "^3.0.0",
    "@aws-sdk/client-sns": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^18.0.0",
    "typescript": "^4.9.0"
  }
}
